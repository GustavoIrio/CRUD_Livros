<div class="book-page">
    <div class="container">
        <h1>{{book.title}}</h1>
        <p>Este livro tem {{book.pageqty}} páginas.</p>
        <div class="actions">
            <form action="/books/edit/{{book.id}}" method="get" class="editar">
                <input type="submit" value="Editar">
            </form>
            <form action="/books/remove/{{book.id}}" method="post">
                <input type="submit" value="Remover">
            </form>
        </div>
    </div>
</div>